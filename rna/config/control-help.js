/** RNA control explanations follow the implemented selection and statistics. */
export const RNA_CONTROL_HELP = Object.freeze({
  methodGroup: 'Select one or more experimental methods. With none selected, all methods are included. Observations are pooled; entries are not given equal weight.',
  contextGroup: 'Choose recorded base, pair, or step contexts for this family. All contexts includes every recorded context; matching sequence text does not establish observation identity.',
  terminalGroup: 'Include ends retains available finite terminal measurements. Exclude ends removes observations marked terminal, including a pair or step with a terminal endpoint. Missing atoms or covalent neighbors still make dependent measurements unavailable.',
  groupingGroup: 'Each curve is normalized separately. Function and structure tags can overlap, so one observation can appear in several curves; adding their counts would double-count it. All observations pools the selected finite measurements.',
  circularModeGroup: 'Applies only to parameters declared circular. Auto places the display seam in a low-density region; 0–360° and ±180° choose fixed seams. Bond angles and other nonperiodic degree-valued parameters remain linear. Raw measurements are unchanged.',
  smoothingSigmaGroup: 'Gaussian width in histogram-bin units, not degrees or ångströms. Off shows unsmoothed bins. Smoothing wraps across circular boundaries. Changing histogram detail also changes the physical width at a fixed σ.',
  displayScaleGroup: 'Probability sums to 1 over bins for each nonempty curve or joint plot. Density divides by bin width in 1D or bin area in 2D, so its integral is 1. Normalization uses the plotted, optionally smoothed measurements.',
  traceStyleGroup: 'Filled and Line only show the same 1D distribution values. The filled area is a display style, not an uncertainty interval.',
  binDetailGroup: 'In 1D, Standard uses 64 linear or 72 circular bins; Fine doubles these counts. In 2D, Standard uses 36 bins per axis and Fine uses 72. Finer bins do not add observations or measurement precision.',
  jointJoinModeGroup: 'Same observation matches explicit IDs at the same residue, pair, or step level. Pair → Residue uses recorded endpoint links between one pair parameter and one residue parameter, in either axis order. Repeated endpoints are related observations, not independent samples.',
  jointResidueSideGroup: 'nt1 and nt2 are the first and second endpoints in the recorded pair ordering, not strand numbers. Both can contribute two finite endpoint observations per pair; each retained endpoint has equal weight.',
  jointPlotTypeGroup: 'Heatmap colors show binned probability or density. Contours connect equal displayed values; filled contours shade those levels. These are descriptive distribution plots, not confidence regions.',
  jointContourLabelsGroup: 'Labels show contour levels and apply only to plots with contours. With Log color, the labels are log₁₀ values; hover still shows the actual probability or density.',
  jointContourWidthGroup: 'Wide shows fewer contour levels; Tight shows more. This changes contour spacing, not line thickness, histogram bins, or observations. It applies only to plots with contours.',
  jointColorScaleGroup: 'Linear colors show probability or density directly. Log colors show log₁₀ of those values with a display floor of 10⁻⁸, including empty bins. Hover and exported values retain the actual values, including zero.',
  jointPaletteGroup: 'Changes the colors used for the joint distribution. The selected color scale, bin values, and observations are unchanged.',
  baseGeometryMinObsGroup: 'A term/context comparison meets the minimum only when all three opening bins contain at least this many finite observations. Comparisons below the minimum remain listed. This coverage threshold is not a significance test.',
  familySelect: 'Families contain residue, pair, or step observations. Available atoms and geometry determine which parameters can be measured; selecting a family does not guarantee finite values for every row.',
  parameterSelect: 'Only measurements with an available finite value enter the plot. Coverage is parameter-specific; missing or unsupported measurements are not replaced by zero. Counts describe observations, not independent RNA molecules.',
  family2Select: 'Same observation requires matching observation levels. Pair → Residue requires a pair family and a residue family. Explicit identities and endpoint links determine the matches.',
  parameter2Select: 'Pearson r and r² describe two linear parameters. Two circular parameters use circular correlation; a mixed circular/linear correlation is not reported. Correlations describe the retained observations and do not establish statistical significance.',
  surveyOpeningSelect: 'Opening bins use the dataset’s declared pair-opening boundaries. A residue linked to multiple selected pairs can contribute once per explicit pair endpoint. These descriptive bins are not RNA conformation thresholds.',
});
